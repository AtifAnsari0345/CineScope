/* ── Movie Components ──
 *
 * This directory contains movie-related UI components:
 *   - MovieCard.jsx    → Individual movie poster card
 *   - PosterGrid.jsx   → Grid layout for movie posters
 *   - MovieBadge.jsx   → Genre / rating badges
 *
 * Components will be created in Step 3.
 */
