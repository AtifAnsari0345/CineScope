/**
 * Custom Hooks
 *
 * Reusable React hooks for the app:
 *   - useWatchlist()   → watchlist state management
 *   - useSearch()      → live search logic
 *   - useMovies()      → movie data fetching
 *
 * Will be implemented as needed in Steps 3–8.
 */

// Placeholder
export * from './usePWA';
export * from './useOnlineStatus';
