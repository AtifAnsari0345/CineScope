/**
 * AppContext — Global application state
 *
 * Manages shared state across the app:
 *   - watchlist (movies the user wants to see)
 *   - watched list (movies already seen)
 *   - user reviews & ratings
 *   - user profile data
 *
 * Will be implemented in Step 3–4.
 */

// Placeholder — context provider will be added here
export default {}
